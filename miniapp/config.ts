// Replace before release with a HTTPS domain registered as a legal request
// domain in the WeChat Mini Program administration console.  Do not append a
// slash and never put server secrets or provider tokens in this file.
export const API_BASE_URL = "https://api.example.invalid";
