Page({
  data: {
    disclaimer: "本服务仅提供研究与教育用途的信息，不构成个性化投资建议、收益承诺或交易指令。"
  },

  onShow() {
    // Future extension point: fetch per-user quota and consent settings from
    // an authenticated profile endpoint.  No provider credentials are ever
    // displayed or configured in the mini-program.
  }
});
