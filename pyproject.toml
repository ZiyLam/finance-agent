[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-ai-agent-framework"
version = "0.1.0"
description = "A provider-agnostic, tool-capable AI agent starter framework."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Your team" }]
dependencies = ["baostock>=0.9.3,<1.0", "yfinance>=1.5.2,<2.0"]

[project.scripts]
ai-agent = "ai_agent.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
