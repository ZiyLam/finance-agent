globalThis.FINANCE_AGENT_CONFIG = Object.freeze({
  // Leave blank for FastAPI-hosted development; use an absolute URL when the
  // static Web app is deployed separately, for example "https://api.example.com".
  apiBaseUrl: "",
});
